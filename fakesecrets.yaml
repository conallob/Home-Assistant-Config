# Fake secrets file for GitHub Actions CI validation
# This file contains dummy values that match the keys in secrets.yaml
# but don't expose real credentials

redfish_user: fake_user
redfish_password: fake_password

timeular_api_key: fake_api_key_value_here
timeular_api_secret: fake_api_secret_value_here

pvoutput_api_key: fake_pvoutput_api_key_value
pvoutput_system_id: "12345"

solis_portal_username: fake@example.com
solis_portal_password: fake_password_here
solis_portal_plant_id: "123456"
